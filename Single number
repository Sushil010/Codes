class Solution:
    # @param A : tuple of integers
    # @return an integer
    def singleNumber(self, A):
        pro=A[0]
        for i in range(1,len(A)):
            pro=pro ^ A[i]
        return(pro)
