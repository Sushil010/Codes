class Solution:
    # @param A : integer
    # @return an integer
    def solve(self, A):
        count=0
        while((A & 1)!=1):
            A=A >> 1
            count=count+1
        return(count)
